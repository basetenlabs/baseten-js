/**
 * Generated TypeScript types for Baseten model configuration (also known as
 * Truss config).
 *
 * These types are generated from the upstream Truss config schema and are NOT
 * covered by stability guarantees. They may change without notice when the
 * upstream schema changes. Pin a SDK version if stability matters.
 *
 * @packageDocumentation
 */
export type * from "./models.gen";
