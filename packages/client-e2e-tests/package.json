{
  "name": "@basetenlabs/client-e2e-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint && oxfmt --check . && tsc --noEmit",
    "format": "oxfmt --write .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@basetenlabs/client": "workspace:*"
  }
}
